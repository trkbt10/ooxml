.DS_Store
_build/
target/
.mooncakes/
.moonagent/

references/
.kiro/
tmp/
out/

# Rendering-snapshot / catalog harness — generated artifacts.
# The catalog tool (src/cmd/catalog) and scripts/snapshot.sh are
# tracked; everything under .snapshots/ is regenerated by them
# (`moon run src/cmd/catalog`, `moon run src/cmd/catalog -- fixtures`,
# `scripts/snapshot.sh`) and is not committed.
.snapshots/
