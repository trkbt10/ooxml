///|
/// XML 1.0 reader/writer (W3C REC-xml).
///
/// Implementation pending. This module will provide:
///   - DOM-style `Document` / `Element` / `Attribute`
///   - Streaming reader (pull-based) for large parts
///   - Writer with namespace-aware prefix bookkeeping
pub fn package_name() -> String {
  "trkbt10/ooxml/xml"
}
