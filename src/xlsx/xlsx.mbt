///|
/// Public facade for .xlsx files. Orchestrates opc + spreadsheet_ml + drawing_ml: open(bytes) → context → (save | to_html).
///
/// Implementation pending.
pub fn package_name() -> String {
  "trkbt10/ooxml/xlsx"
}
