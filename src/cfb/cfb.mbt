///|
/// Compound File Binary (MS-CFB) container for legacy pre-OOXML Office formats
/// (.doc, .xls, .ppt). Not part of ECMA-376; this package is reserved for a
/// future migration path and exposes no public API in this build.
pub fn package_name() -> String {
  "trkbt10/ooxml/cfb"
}
