///|
/// MS-CFB compound file reader/writer.
///
/// Implementation pending. Used for:
///   - Embedded OLE objects inside OOXML packages
///   - Future support for legacy .xls / .doc / .ppt
pub fn package_name() -> String {
  "trkbt10/ooxml/cfb"
}
