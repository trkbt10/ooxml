// trkbt10/ooxml/edit
//
// A-layer generic XML CRUD primitives. Provides namespace-aware
// search (find_first / find_all), structural mutations (insert /
// remove / replace / map_subtree), and attribute updates. These are
// the building blocks every higher-level facade operation must use.

import {
  "trkbt10/ooxml/xml" @xml,
}

import {
  "moonbitlang/core/debug" @debug,
} for "wbtest"
