// WASM build target for the .pptx facade.

import {
  "trkbt10/ooxml/pptx" as @pptx,
}

options(
  link: {
    "wasm-gc": {
      exports: [
        "pptx_package_name:package_name",
      ],
    },
  },
)
